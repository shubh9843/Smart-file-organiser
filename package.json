{
  "name": "smart-file-organiser",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node server.js",
    "install-all": "cd backend && npm install",
    "build": "cd backend && npm install"
  },
  "dependencies": {}
}
